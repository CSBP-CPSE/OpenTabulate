"""
* Statistics Canada - Center for Special Business Projects - DEIL *

~ Maksym Neyra-Nesterenko

This script serves as a template for address parsing.

"""
"""
import pyap as ap

test_address = input() + input()

addresses = ap.parse(test_address, country='CA')

for address in addresses:
    print(address.as_dict())
    type(address.as_dict())
"""

stdin = input() + input()
print(stdin)
